Router.map(function () {
    this.route('home', {
        path: '/home',
    }),
    this.route('articleForm', {
        path: '/addarticle',
    }),
        this.route('reservationForm', {
        path: '/reservation',
    })
});